$ ->
  if window.controller == "student/notes"
    $("#app-user-navbar .nav .notes").addClass("active")
  if window.controller == "student/papers"
    $("#app-user-navbar .nav .papers").addClass("active")

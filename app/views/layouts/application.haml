!!!
%html
  %head
    %title 易飞网
    %meta{content: "width=device-width, initial-scale=1.0", name: "viewport"}/
    %meta{"http-qeuiv" => "X-UA-Compatible", content: "IE=edge"}/
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "application"
    = yield :app_head
    = javascript_include_tag "respond.src"
    = javascript_include_tag "html5shiv"
    = csrf_meta_tags
  %body
    #app-notification.jesse-notification
      - if @flash.present?
        %span= @flash
      - else
        %span= (flash.map { |name, msg| msg }).join(', ')
    #wrap
      = yield :app_nav
      = yield :app_content
      #push
    #footer
      .container
        %p.muted.credit
          - if Rails.env != "production"
            %span= Rails.env
          = link_to "ChangeLog", { controller: "/teacher/homeworks", action: "index" }, :method => "get"
          %span= "|"
          = link_to "Feedback", { controller: "/teacher/homeworks", action: "index" }, :method => "get"
